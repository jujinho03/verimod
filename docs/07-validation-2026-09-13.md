# 실제 검증 기록 — 2026-09-13

기준: 시작 HEAD/origin/main `908f64eec5933dce2371ca48d35893fa01d0a8e8`, branch `docs/current-context-and-interface-draft`. 기록된 결과는 이 브랜치 P0 작업 트리에서 실행했다. 정확한 반영 commit은 이 파일의 Git 이력과 PR을 따른다.

## 환경·설치

Windows / Node **24.19.0** / npm **12.0.2**. npm이 PATH에 없어서 공식 npm registry의 npm 12.0.2 tarball을 SHA-1 검증 후 repository 밖 작업 폴더에 추출했다. 실제 호출은 `node <external npm>/bin/npm-cli.js <script>`이며 package.json의 같은 npm scripts를 실행한다. subprocess에도 Node 실행 경로를 전달했다. 프로젝트 dependency/lockfile은 변경하지 않았다.

| 명령 (해당 폴더) | 실제 결과 |
|---|---|
| frontend: npm ci | exit 0, 55 packages, audit 0 |
| backend: npm ci | exit 0, 145 packages, audit 0 |
| backend/contracts: npm ci | exit 0, 263 packages, audit 15 entries |
| frontend: npm test | **9 files / 96 tests passed**, 최종 21:40 KST |
| frontend: npm run lint | exit 0, **기존 경고 8개**; warning-free 아님 |
| frontend: npm run build | exit 0, tsc + Vite, 86 modules |
| backend: npm test | **1 file / 1 health test passed**, 21:39 KST |
| backend: npm run typecheck | exit 0 |
| backend: npm run build | exit 0 |
| backend/contracts: npm test | **1 Mocha test passed** (ToolchainCheck deploy/ping) |
| backend/contracts: npm run build | exit 0; 기존 cache에서 No contracts to compile |
| 최초 contract compile | solc **0.8.34**, 1 Solidity file, EVM target osaka |
| git fetch origin | 성공, 최종 재조회에서도 origin/main = 908f64e |
| git diff --check / git status --short | diff --check exit 0; status는 이번 P0 수정/추가 파일만 표시 (commit 전) |

Contract test의 deploy는 **Hardhat 테스트 환경의 ToolchainCheck**다. 실제 VeriMod 계약·EVM testnet 배포가 아니다.

## 수정 전·수정 중 결과와 구분

수정 전 baseline: frontend 7파일 54건, backend 1건, contract 1건 통과. Frontend/backend build와 backend typecheck 성공. 기존 frontend lint 경고 8개.

최초 npm test 시도는 Windows subprocess가 Node 경로를 찾지 못해 실행 전 실패했다. PATH를 보정한 뒤 위 결과를 얻었다. 개발 중 새 테스트 1건은 fixture의 content commitment가 달라 원하는 검증 분기에 도달하지 못했고, 테스트의 Node crypto 타입 참조가 빠져 build가 한 차례 실패했다. fixture/테스트 타입을 고친 뒤 재실행했다. 이를 통과 결과에 숨겨 합산하지 않는다.

npm 12는 esbuild postinstall을 allowScripts 미승인으로 차단했다고 경고했다. 현재 설치된 platform package로 명령은 통과했다. 이 경고를 해소했다고 주장하지 않는다. Contracts의 deprecated glob/inflight 경고도 기록한다.

## 실제 브라우저 확인

Chrome 연결 브라우저, Vite `http://127.0.0.1:5173`. 인앱 브라우저는 사용 불가여서 Chrome을 사용했다. 작업 재개 시 개발 서버를 다시 실행한 뒤 검증을 이어갔다.

| 경로/시나리오 | 확인 결과 |
|---|---|
| / | 화면 열림, 첫 화면 Prototype mode·Synthetic moderation·Simulated ledger |
| /check | 제한 예시 실행, synthetic spam 990000 / RESTRICT, 영수증 발급 |
| /receipts/:receiptId | 새 DECISION 상세, 합성 epoch #4 포함·confirmation |
| /verify | 실제 새 영수증 **VALID**, hash·proof·합성 원장·finality 통과 |
| 사본 변조 | seed 사본 **HASH_MISMATCH**, 실제 화면 캡처 |
| 사본 수정 후 재해싱 | 해시 통과·포함 증명 단계 실패 (INVALID_PROOF 경로) |
| RPC 장애 토글 | **RPC_UNAVAILABLE**, 변조 아님 안내; 이후 토글 복구 |
| appeal | 새 RESTRICT receipt에 합성 본문 접수, APPEAL 영수증 발급 |
| /review | 새 appeal 대기열, OVERTURN → ALLOW 및 사유 기록, REVIEW 발급 안내 |
| /receipts | 새 DECISION/APPEAL/REVIEW와 검토 완료 목록 표시 |
| /protocol | Implemented today / Simulated today / Next integration, 평문 저장·권한 한계 표시 |

브라우저 새 DECISION ID: `0a90f51f-1509-47f3-985b-6dfc6595535d`, REVIEW ID: `4a9e53f5-3273-4c2f-bf47-5c71872f8fc9`. 합성 검증용 기록이며 사용자 실제 moderation 사건이 아니다. 이 브라우저의 localStorage에만 있다. 새 REVIEW 발급 후 최종 anchor/lifecycle까지의 브라우저 E2E는 별도 실행하지 않았으며, unit/integration에서는 seed lifecycle 및 신규 store 경로를 검증했다.

실제 캡처: [홈](assets/p0-home-2026-09-13.jpg), [판정](assets/p0-check-2026-09-13.jpg), [변조](assets/p0-tamper-2026-09-13.jpg). 이미지 생성·합성 screenshot을 사용하지 않았다.

## 재현성·독립 계산 증거

- P0 수정 전 seed hash 5개·epoch root 3개를 외부 파일에 보존하고 hardening.test에 고정했다. 최종 모두 동일.
- production recursive Merkle 코드와 별도의 iterative frontier + Node createHash로 n=1..32 root와 모든 index proof를 대조했다.
- 필수 n=1/2/3/4/5/7/8/9 모두 포함. Merkle root만으로 count를 완전히 구분하지 못하는 경우는 기존 테스트를 유지하고 verifyCore의 trusted count 확인으로 막는다.
- 같은 synthetic 입력의 점수는 고정, 새 receipt UUID/시각/salt는 의도적으로 새 값. 홈 시각 효과는 Math.random을 사용하여 screenshot의 티커 순간 프레임은 다를 수 있다.
- 모든 언어·런타임·독립 구현의 protocol 상호운용 인증을 수행한 것은 아니다.

## CI

[ci.yml](../.github/workflows/ci.yml)은 세 package job만 사용한다. Node 24.19.0/npm 12.0.2, 각각 npm ci/test 및 frontend lint/backend typecheck/build. local 검사가 통과한 후 추가했다. token permission contents:read, credentials 미보존, deploy/secret 사용 없음.

[actions/checkout](https://github.com/actions/checkout)과 [actions/setup-node](https://github.com/actions/setup-node) 공식 v7 태그를 조회하고 SHA를 고정했다. **이 문서 최초 작성 시 GitHub Actions 실행 결과는 아직 없음**. push 이후 결과는 PR checks/run 링크에서 확인하며 로컬 통과를 원격 CI 통과로 표기하지 않는다.

### GitHub 반영 후 확인

P0 코드 commit: `bee347f00f1795d47063aac281411195ee322346`. 기능 브랜치 push 완료, [PR #1](https://github.com/jujinho03/verimod/pull/1) 생성. main 자동 병합은 하지 않았다.

[Push CI run 34758109129](https://github.com/jujinho03/verimod/actions/runs/34758109129): **completed / success**, frontend·backend·contracts 세 job과 해당 test/lint/typecheck/build step 성공을 GitHub API에서 확인했다. Ubuntu runner에서도 설치·검증이 통과했다.

[PR CI run 34758193977](https://github.com/jujinho03/verimod/actions/runs/34758193977)도 같은 코드 SHA에서 **completed / success**로 확인했다.

최초 git push는 Git credential helper 선택 창에서 진행되지 않았다. 해당 대기를 종료하고 명령 범위에서 설치된 credential manager를 지정해 push했다. 전역 인증 설정이나 권한은 변경하지 않았다. GitHub connector PR 생성은 403으로 실패하여 로그인된 GitHub 웹 화면에서 PR을 생성했다.

## 남은 경고와 미실행

- Lint 경고 8개: HashBand, SiteHeader, Reveal(2), Home(2), VerifyReportView, ScoreBars. 제출 전 무관한 animation/refactor로 확대하지 않음.
- Contracts npm audit: 12 low / 2 moderate / 1 high, 총 15 package entries. serialize-javascript와 Mocha 개발 경로, adm-zip/diff/elliptic 및 전이 의존성. [audit](05-protocol-audit.md)의 영향 범위 참고. 취약점 0 상태 아님, major 자동 upgrade 안 함.
- 실제 AI 학습/성능/데이터 다운로드·라이선스 채택: **실행 안 함**.
- 업무 backend·DB·authenticated reviewer·실제 EVM contract/testnet·explorer 확인·실제 chain/reorg E2E: **실행 안 함**.
- 모바일·다중 브라우저·부하/fuzzing·production 배포·영상/GIF·외부 침투 테스트: **실행 안 함**.
- PDF snapshot 재생성: **실행 안 함**; Markdown 원본 갱신, 기존 PDF와 PPTX reference 보존.
- LICENSE 선택: **실행 안 함**. Repo description 확인, topics는 비어 있음; 자동 태그·라이선스 변경 없음.

## 2026-09-14 제출 자료 최종 점검

시작 commit `8b9a45f05f68db08e41368fd0f836314cdcf78f0`에서 제출 안내·공개 JSON 예제·예제 회귀 검사를 추가했다. 제품 runtime과 기존 hash 규칙·dependency/lockfile은 변경하지 않았다. main은 재조회에서도 `908f64e`였으며 PR #1의 팀원 리뷰는 없었다.

| 실제 실행 (02:38~02:39 KST) | 결과 |
|---|---|
| 기존 buildSeedState + locatorOf로 공개 bundle 추출 후 verifyReceipt 실행 | VALID / HASH_MISMATCH / INVALID_PROOF 확인 |
| frontend npm test | **10 files / 101 tests passed** (기존 96 + 공개 예제 5) |
| frontend npm run lint | exit 0, 기존 warning 8개 |
| frontend npm run build | exit 0, 86 modules |
| backend npm test / npm run typecheck / npm run build | 테스트 1건·typecheck·build 모두 exit 0 |
| contracts npm test / npm run build | ToolchainCheck Mocha 1건·build exit 0 |

원본 bundle은 기존 seed에서 추출했고 점수 변경·재해싱 사본은 시험용으로 의도적으로 만들었다. 원문·salt·appeal 본문은 공개 JSON에 없다. 공개 파일 자체를 읽는 [submission.test.ts](../frontend/src/domain/submission.test.ts)가 원본 seed와의 일치, 명시한 필드만의 변경, RPC_UNAVAILABLE 구분을 검사한다. Node에서의 검증 결과이며 이번 추가 JSON 파일의 브라우저 파일 선택 동작을 다시 실행한 결과는 아니다. 이전 브라우저 검증·실제 screenshot은 위 9월 13일 기록을 따른다.

이번 마무리에서는 npm ci를 재실행하지 않고 앞선 lockfile 설치를 사용했다. 새 commit의 CI는 깨끗한 runner에서 각 npm ci와 필수 검사를 실행하며, 정확한 최종 결과는 [PR #1 checks](https://github.com/jujinho03/verimod/pull/1/checks)를 따른다. 이전 CI 통과를 새 commit 결과로 대체하지 않는다. 실제 AI·testnet·웹앱 배포·PDF 재생성은 여전히 실행하지 않았다.
